import { Skeleton } from "@radix-ui/themes";

export const TaskSkeleton = () => (
  <Skeleton className="relative w-full bg-white p-4 rounded shadow mt-1 border-b border-slate-300 max-w-2xl"></Skeleton>
);
